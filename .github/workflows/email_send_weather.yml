# 参考 https://github.com/ruanyf/weather-action

name: 'GitHub Actions Weather Bot'

on:
  push:
  schedule:
    - cron: '0 21 * * *'

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v1
#       - name: 'Get Weather'
#         run: bash ./weather.sh
      - name: 'Set env'
        env:
          REPORT_DATE: date '+%Y-%m-%d %T'
          CITY: 北京市
          LANGUAGE: "zh-CN"
          UA: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/79.0.3945.88 Safari/537.36"
        run: curl -H "Accept-Language:$LANGUAGE" -H "User-Agent:$UA" -o result.html wttr.in/$CITY
      - name: 'Send mail'
        uses: dawidd6/action-send-mail@master
        with:
          server_address: smtp.163.com
          server_port: 465
          username: 13526636962@163.com
          password: ${{ secrets.EMAIL163_PWD }}
          subject: 天气预报 Report ( $REPORT_DATE )
          html_body: file://result.html
          to: 897379293@qq.com
          cc: 710347627@qq.com
          from: GitHub Actions
